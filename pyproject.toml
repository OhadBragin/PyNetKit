[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pynetkit"
version = "1.0.0"
description = "A powerful, Nmap-inspired network discovery and security auditing toolkit with a Rich CLI and modern GUI."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"
authors = [
    { name = "Developer" }
]
dependencies = [
    "scapy==2.7.0",
    "rich==14.3.3",
    "rich-argparse==1.7.2",
    "ttkbootstrap==1.20.2"
]
keywords = ["network", "security", "scanner", "arp-spoofing", "dos", "cli", "gui", "scapy", "rich"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Security",
    "Topic :: System :: Networking :: Monitoring"
]

[project.urls]
Homepage = "https://github.com/OhadBragin/pynetkit"

[project.scripts]
pynetkit = "pynetkit.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
